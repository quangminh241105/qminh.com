services:
  mongodb:
    image: mongo:7.0
    container_name: qminh-mongodb
    restart: unless-stopped
    ports:
      - "27017:27017"
    environment:
      MONGO_INITDB_ROOT_USERNAME: admin
      MONGO_INITDB_ROOT_PASSWORD: password
      MONGO_INITDB_DATABASE: portfolio
    volumes:
      - mongo-data:/data/db

volumes:
  mongo-data:
